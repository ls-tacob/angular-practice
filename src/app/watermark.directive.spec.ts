import { WatermarkDirective } from './watermark.directive';

describe('WatermarkDirective', () => {
  it('should create an instance', () => {
    const directive = new WatermarkDirective();
    expect(directive).toBeTruthy();
  });
});
