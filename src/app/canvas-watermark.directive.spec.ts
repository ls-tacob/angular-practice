import { CanvasWatermarkDirective } from './canvas-watermark.directive';

describe('CanvasWatermarkDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasWatermarkDirective();
    expect(directive).toBeTruthy();
  });
});
